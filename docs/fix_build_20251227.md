# Correctif build du 2025-12-27

- Problème : double déclaration de `largest_dma` dans `components/rgb_lcd_port/rgb_lcd_port.c` empêchant la compilation.
- Correction : renommage de la variable utilisée pour le dimensionnement des bounce buffers et conservation du log existant sans modifier la logique.
- Nettoyage warnings :
  - Encapsulation du guard PCLK (lock + profondeur) et de son getter sous `CONFIG_ARS_LCD_PCLK_GUARD_ENABLE`.
  - Neutralisation des warnings `unused` dans `components/sd/sd_host_extcs.c` pour les diagnostics MISO/CS et la fonction de readback conditionnelle.
- Build : commandes prévues `idf.py fullclean` puis `idf.py build` (non exécutées ici car `idf.py` absent dans l’environnement); la double déclaration est corrigée côté source.
